import os

class Config:
    SECRET_KEY = 'your-secret-key'  # Change this to a random secret key
    POCKETBASE_URL = 'http://127.0.0.1:8090'  # Replace with your actual PocketBase URL
    # Add other configuration settings as needed